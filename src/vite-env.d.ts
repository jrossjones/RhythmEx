/// <reference types="vite/client" />

// Injected by Vite `define` at build time (see vite.config.ts).
declare const __APP_VERSION__: string // short git hash
declare const __BUILD_DATE__: string // UTC build timestamp "YYYY-MM-DD HH:MM"
